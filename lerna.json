{
  "packages": ["packages/*", "apps/*", "services/*"],
  "version": "independent",
  "npmClient": "yarn",
  "command": {
    "version": {
      "allowBranch": "master"
    },
    "publish": {
      "allowBranch": "master"
    }
  }
}
